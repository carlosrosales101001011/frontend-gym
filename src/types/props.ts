export type isOpenModalCustom ={
    isOpen:boolean,
    id:number,
    isCopy:boolean
}

export type modalCustom ={
    id:number,
    onHide:()=>void,
    show:boolean
}

export type OpcionesSelect = {
    value:number,
    label:string
}